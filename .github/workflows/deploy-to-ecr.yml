name: Deploy to ECR

on:
  # push:
  #   branches: [ master ]
  pull_request:
    branches: [ master ]
jobs:
  build:
    name: Build Image
    runs-on: ubuntu-latest
    steps:
    - name: Check out code
      uses: actions/checkout@v2
    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: us-east-1
    - name: Login to Amazon ECR
      id: login-ecr
      uses: aws-actions/amazon-ecr-login@v1

    - name: Build and Push to ECR public
      env:
        ECR_REGISTRY: public.ecr.aws/h7r1e4h2
        ECR_REPOSITORY: chemical-interactions
        IMAGE_TAG: latest
        id: build-and-push
        uses: pahud/ecr-public-action@8cd826db40befb59b0cd0b60b22a7ba72d06a7f7
        with:
          dockerfile: docker/Dockerfile
          context: .
          tags: |
            $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
    
    # - name: Build, tag, and push image to Amazon ECR
    #   env:
    #     ECR_REGISTRY: public.ecr.aws/h7r1e4h2
    #     ECR_REPOSITORY: chemical-interactions
    #     IMAGE_TAG: latest
    #   run: |
    #     docker build --file docker/Dockerfile -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .
    #     docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
