name: Validate Pull Requests
on:
  pull_request:
    branches: [ master ]

jobs:
  run-tests:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: pip install -r chem_interactions/requirements.txt
    - run: python run_tests.py
